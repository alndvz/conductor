# Tasks

- [x] Test conductor-plugin plugin
- [x] Verify file change detection
- [x] Confirm TICK messages arrive
- [x] Test file change notification with conductor
- [x] Route all messages through sendOrQueue
- [x] Include diff in file change notifications
- [x] Extract pure functions from conductor-plugin.ts: pull out message queueing logic (shouldQueue, enqueue, dequeue), session state transitions (addSession, removeSession, setStatus), notification formatting, and git helpers (gitHash, gitDiff) into separate testable modules. Keep the plugin as a thin orchestrator wiring them together.
- [x] Consolidate git-helpers.ts, message-queue.ts, notifications.ts, and session-state.ts back into conductor-plugin.ts. Keep the functions modular and well-organized within the file, but delete the four separate modules. Update imports accordingly.
- [x] Create an `/install-conductor` command at `.opencode/commands/install-conductor.md` that installs the conductor workflow into a target repo. The command receives `$ARGUMENTS` as the target directory path. The agent should read the necessary files from the current (source) repo and copy/adapt them into the target: copy `conductor-plugin.ts` to `.opencode/plugins/`, copy the four agent definitions to `.opencode/agents/`, merge the `agent` section into the target's `.opencode/opencode.json` (don't blindly overwrite), scaffold `TASKS.md` if missing, copy `conductor.sh` and make it executable, and run `npm install` for `@opencode-ai/plugin` and `@opencode-ai/sdk`. Create directories as needed and report each action taken.
- [x] Add `"permission": "allow"` to the conductor, feature, implementor, and review agent configs in `.opencode/opencode.json` so the conductor workflow runs without permission prompts. Scope to these four agents only (not global), so built-in agents retain their defaults.
- [x] Remove `"permission": "allow"` from the feature, implementor, and review agent configs in `.opencode/opencode.json` — keep it only on the conductor.
- [x] Rework the Feature agent to be conversational-first with planning support. Update `.opencode/agents/feature.md`: default behavior is to discuss, ask clarifying questions, and NOT immediately write tasks — only write tasks once the user confirms or the discussion converges. Add planning mode: entered when the user mentions planning or the agent detects the request is multi-step/complex. While planning, delegate codebase exploration to the explore sub-agent (use the Task tool) to avoid filling Feature's context. The plan lives in context, then gets flushed to TASKS.md — incrementally as parts solidify if possible, all at once otherwise. Also add `"Task": true` to the feature agent's tools in `.opencode/opencode.json` so it can delegate to the explore sub-agent.
- [x] Create `.opencode/agents/rules-review.md` as a new read-only sub-agent. Embed the 6 domain rules (event-payload-validation-in-module, rules-must-be-side-effect-free, schemas-must-use-current-namespace-alias, datomic-schema-placement, dispatch-paths-must-have-real-dependency-evidence, tiny-stable-shapes-must-not-be-abstracted-for-dry) with their descriptions. Instruct the agent to: run `git diff` to see changes, evaluate each rule against the diff independently, and report a markdown table with columns Rule/Mark/Reason — one row per rule — followed by an overall verdict of APPROVED (all PASS) or CHANGES REQUESTED (any FAIL). Follow the same structure and conventions as `.opencode/agents/review.md` (read-only tools, report-back format, stateless).
- [x] Register the `rules-review` sub-agent in `.opencode/opencode.json` under the `agent` section with `mode: "subagent"`, `model: "opencode-go/deepseek-v4-flash"`, and the same read-only tools as the review agent (Bash, Read, Glob, Grep).
- [x] Fix `.opencode/agents/rules-review.md`: replace the 6 events-domain rules with the 2 concrete rules below. Change the model in the preamble from `opencode-go/deepseek-v4-pro` to `opencode-go/deepseek-v4-flash`. Remove SKIP as a verdict option — every rule always applies, use only PASS/FAIL. Rule 1 (simplicity): "Implementations must be as simple as possible, but no simpler — the diff must not introduce premature abstraction, unnecessary indirection, or complexity without clear justification." Rule 2 (minimal-diff): "The diff must be minimal and self-contained — no reformatting, no refactoring, no changes to unrelated code, no stray debug prints, TODO comments, or commented-out code."
- [x] Update `.opencode/agents/conductor.md` to run the general review and the rules review concurrently after implementation. After the implementor reports completion, launch both `subagent_type: "review"` and `subagent_type: "rules-review"` in parallel Task tool calls within a single message. Wait for both verdicts before committing. Document the rules-review sub-agent in the "Using sub-agents" section alongside the existing reviewer.
- [ ] Add plan support and parallel implementation to conductor and feature agents — see conductor-plans/dual-review-and-plan-support.md (updated)
